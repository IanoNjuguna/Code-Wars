#include <stdio.h>

/**
 * opposite - Finds the opposite of a number
 *
 * @num: Contains the value of the number
 * Return: - num (Opposite of the number)
 */

float opposite(float num)
{
	return (-num);
}

